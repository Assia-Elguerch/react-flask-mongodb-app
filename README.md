# react-flask-mongodb-app
This repository contains a containerized full-stack web application built with React (frontend), Flask (backend API), and MongoDB (database), fully orchestrated using Docker and automated through a Jenkins CI/CD pipeline.
